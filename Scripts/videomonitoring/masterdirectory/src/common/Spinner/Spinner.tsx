import * as React from "react";
import "./Spinner.scss";

const Spinner = () => {
	return (
		<div className="spinner_box">
			<div className="spinner"></div>
		</div>
	);
};

export default Spinner;
